# boardid.config

# Read the serial number from the U-boot environment block.
-b uboot_env -u nerves_serial_number
-b uboot_env -u serial_number

# Fall back to the MAC address of the first Ethernet port.
-b macaddr
